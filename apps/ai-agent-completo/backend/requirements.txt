# Dependências do Backend Smart Kaits
# Gerado para Python 3.11+
# Instale com: pip install -r requirements.txt

# ── Web Framework ──────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0      # Servidor ASGI com suporte a async

# ── Validação e Settings ───────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1       # Leitura de .env via BaseSettings

# ── Banco de Dados (async) ─────────────────────────────────
sqlalchemy[asyncio]==2.0.30    # ORM com suporte a async
asyncpg==0.29.0                # Driver PostgreSQL async
alembic==1.13.1                # Migrations de banco

# ── Autenticação ───────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT (encode/decode)
passlib[bcrypt]==1.7.4             # Hash seguro de senha

# ── Upload de Arquivos ─────────────────────────────────────
python-multipart==0.0.9            # Suporte a multipart/form-data no FastAPI

# ── Utilitários ────────────────────────────────────────────
python-dotenv==1.0.1               # Carregar .env manualmente se necessário
httpx==0.27.0                      # Cliente HTTP async (para testes e LLM futura)

# ── Inteligência Artificial e RAG ──────────────────────────
openai==1.30.1
qdrant-client==1.9.1
pymupdf==1.24.4             # Para leitura de PDF (fitz)
python-docx==1.1.2          # Para leitura de DOCX

# ── Desenvolvimento ────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
httpx                              # Também usado nos testes via TestClient
