# Ative para consumir /api/metricas via HTTP em vez do mock em memória.
USAR_HTTP_KPI=0

# Base pública usada pelo cliente HTTP no servidor (SSR) ao chamar a API interna.
NEXT_PUBLIC_APP_URL=http://127.0.0.1:3000
